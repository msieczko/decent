import {Router} from 'express'
import {Services} from '../services'
import {Config} from '../config/Config';

export function detailsRouter (services: Services, config: Config) {
  const router = Router();


  return router
}
